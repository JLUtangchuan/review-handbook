---
title: "自动驾驶决策规划---时空联合规划"
author: "胖胖橙"
source_url: https://zhuanlan.zhihu.com/p/13376371919
platform: zhihu
collection: "学习收藏"
collection_url: https://www.zhihu.com/collection/713057152
topic: ""
subtopic: ""
status: pending
---

# 自动驾驶决策规划---时空联合规划

> 作者: 胖胖橙 | 来源: https://zhuanlan.zhihu.com/p/13376371919

---

1. 时空联合规划的基本概念

思考： 如何解决窄道会车问题？

问题难点：存在交互，对手车与自车的行为紧密结合。

时空解耦的方法：先生成路径，很难考虑动态障碍物，会生成穿过目标车的轨迹。进行速度规划后，由于有障碍物，会进行刹车。时空解耦实际是为了减小求解空间，减少算力。

窄道会车

时空联合规划方法：

减速，靠边并让出一部分空间，等对手车通过后，再通行 --> 让
认为对手车会让行，加速抢过去 --> 抢

如果对该问题进行建模？

时空解耦实际是为了减小求解空间，减少算力。最简单粗暴的办法是，将解空间完全放开。

1.1 时空分离方法




时空分离方法
先不考虑时间，在x-y平面找到路径
增加时间维度，根据路径生成速度曲面，在速度曲面上计算速度曲线

局限性：初始路径决定了轨迹的求解质量，会存在偶发的误制动。在生成轨迹的时候，未考虑障碍物未来一段时间的变化。

1.2 时空联合方法

在 x-y-t坐标下，或者s-l-t坐标下，直接求解轨迹。

问题： 如何求解？

时空联合规划

难点：

计算复杂度：空间维度提升，dimension。在一个高维的非凸空间内，存在横向绕过，或者纵向加减速，抢让等多种轨迹解。
障碍物交互： 其他障碍物按照预测线去投影，而预测轨迹并完全准确。当自车执行某些动作时，对手车也会变化，预测轨迹会更复杂。同时，如果将预测轨迹投影到三维空间，计算复杂度指数上升。 暴力方法，不适用于障碍物的交互。
2.基于搜索的时空联合规划 （Hybrid A* ）
2.1 三维时空联合地图的构建

不同坐标系适合的场景：

x-y-t：路口、半结构化场景（停车场）、非结构化道路（乡间小路）。 无图会依靠x-y-t 。
s-l-t ：非路口、有明确道路指引、有明确道路结构。（对于路口、U型弯，会存在投影不唯一等问题）

障碍物在三维s-l-t栅格地图中的投影表示了它在不同时间和位置上的状态。

X-Y-Z 视角

车辆在 X、Y 维度上呈现的是物理上的空间位置，反映它们在道路上的横向（车道）和纵向（前后位置）信息。

S-L-T 视角

S（纵向位置）: 在 SLT 坐标系中，S 代表车辆在道路上的前进距离。图中的 V1 和 V2 的 S 轴值对应它们在道路上的前进距离。
L（横向位置）: L 代表车辆在道路上的车道位置，或者横向偏移。两个黄色的动态障碍物分别位于不同的车道，说明它们的 L 值不同。
T（时间）: T 轴代表时间，在规划中表示车辆在某个时间点的位置。通过将障碍物在时间维度上延展，展示其随时间的运动轨迹。

障碍物的投影

静止物体：在时间轴（T）上并没有延展，因为它是静止的，纵向位置（S）保持不变，横向位置（L）也固定。
动态障碍物：在时间轴上有一定的延展，在时间维度上延展为一个“斜坡状”，反映它们在未来的时刻可能出现在的位置。与自车的行驶方向不同，其斜坡的朝向也不同。

如何构建三维时空地图？

先建立二维图层，然后沿时间轴拓展为三维时空地图。

参考 EPSILON --> SSC 部分代码，xyt -> slt的投影，以及构建slt map。

建立二维图层

2. 二维栅格沿时间轴拓展生成为三维时空地图。

object --> predicted trajectory --> projection

三维时空地图

3.有了三维地图，并且知道障碍物在时间空间的分布，如何找到一条时空上可行的轨迹?

将自车位置作为搜索的起点，将车辆运动学信息进行离散化、拓展，得到动作空间，根据动作空间扩展时空地图，在动作空间内进行搜索得到轨迹。

三维时空地图包含的信息
2.2 基于Hybrid A*的时空联合规划

时空节点扩展

基于hybrid A*的时空节点扩展，通过离散曲率（前轮转角）与离散加速度（Acc）得到状态更新方程。

时空节点拓展

时空节点启发式函数设计

启发函数cost：按照时间、距离作为启发项。

s-l-t下搜索的特殊性：s-l-t 是个有向图无环图，时间(t)无法倒流，且对于行车功能，不存在倒车场景，即S不倒流。

启发函数cost定义

时空节点成本函数设计

综合考虑车道居中代价、碰撞代价、舒适性代价等等。

时空节点成本函数设计
2.3 Hybrid A* 生成参考轨迹

Hybrid A* 算法的关键点总结：

以车辆当前的位置 (s_1, l_1, \theta_1, v_1, t_1) 作为初始节点，进行时空扩展。其中，s_1 和l_1 是车辆的位置（纵向和横向），\theta_1 是航向角，v_1 是速度， t_1 是时间。
在扩展过程中，对节点进行有效性检查，确保路径不会与障碍物发生碰撞，并且节点是否满足道路边界约束。
使用代价函数评估候选节点，选择扩展代价最低的节点，优先搜索最优路径。
根据扩展过程中的各个最优节点，得到一条初始的粗略轨迹。
Hybrid A* 生成参考轨迹框架

Hybrid A* 算法的主要步骤

1.输入数据：算法接收一个时空有向无环图（DAG）地图，包含初始位置和目标位置。

2.初始化：

将初始点加入 trajectory（轨迹）集合。
将初始点加入 open set（开放集合）。
根据动态障碍物信息构建不同时间步长的 closed set（关闭集合）。

3.主循环：

当 open set 不为空时，循环执行以下步骤：
估计代价：根据代价函数 Cost对 open set 中每个节点的代价进行估算。
选择节点：从 open set 中选取代价最低的节点作为当前节点 current，并将其作为车辆状态 s_t 。
更新轨迹：将状态 s_t 添加到 trajectory 集合中。
检查是否到达终点： 如果当前节点是目标位置，则说明轨迹成功找到，算法结束。
扩展节点：
将当前节点从 open set 中移除。
生成当前节点在下一个时间步长的邻居节点。
对每个邻居节点：
如果该节点在对应时间步长的 closed set 中，忽略它。
如果不在 open set 中，将其加入 open set。

4. 时间步长更新：时间步 t 增加 1。

5. 返回结果：输出最优的时空轨迹。


2.4 Hybrid A* 算法时空代价复用

在时空联合规划中，由于需要在三维时空空间（包括位置和时间维度）内进行搜索，计算代价往往会消耗较多的计算资源。为了降低搜索耗时，可以通过时空代价复用来优化代价计算。时空代价复用的关键在于减少对重复计算的需求，以下是常用的几种优化思路：

缓存与代价复用
缓存计算过的代价：对于已经计算过的时空节点，存储它们的代价。在后续的搜索过程中，如果访问到同一个节点或其状态变化较小的邻居节点，可以直接复用之前计算的代价，而不需要重新计算。
邻近节点的代价估算：由于相邻时间步的节点在空间和时间上通常变化较小，因此可以通过对相邻节点的代价进行微小的调整（例如加上时间或空间步长的权重），而不是从头开始计算代价。这可以大大减少计算量。

2. 启发式估计改进

启发式函数改进：在 Hybrid A* 中，代价函数通常由实际代价和启发式代价两部分组成。启发式函数可以通过更加准确地预测未来的代价来减少不必要的扩展。
预计算距离表：可以预先离线计算出固定空间中的某些位置对之间的最优代价，并将这些结果存储起来，作为启发式函数的一部分。在在线搜索时直接使用这些结果，而不是实时计算距离或代价。
动态调整启发式权重：根据当前障碍物的动态信息，实时调整启发式估计中的权重，进一步提高搜索效率。

3.稀疏时间层扩展

减少时间层的数量：在时空搜索中，不是每个时间步都必须精确扩展。如果障碍物的动态变化较慢，可以通过跳过中间的时间步来简化计算。即每隔几个时间步（比如 2 或 3 个时间单位）再进行节点扩展，降低搜索规模。
动态时间步长调整：对于动态障碍物较少或移动较慢的区域，可以适当增大时间步长，从而减少每一层时间节点的计算量。在障碍物密集或动态变化较快的区域，可以缩小时间步长，保证路径的精确性。

4.动态规划思想的引入

共享状态的累积代价复用：可以借鉴动态规划（Dynamic Programming，DP）的思想，将计算过的状态代价储存起来，并在相同状态出现时复用之前的结果。特别是在有多个路径可以到达同一位置状态的情况下，可以复用之前计算过的最优代价，避免重复计算。
增量式规划：对于大多数情况下，障碍物的动态变化是渐进的（而不是突发性的）。因此可以在每次障碍物状态更新时，基于先前规划好的路径进行增量式的规划，而不需要完全重新计算整个路径。
3.基于迭代计算的规划方法

暴力搜索存在的潜在问题：

时空联合搜索的轨迹空间过于庞大； 尝试Alternative Minimization？（将一个整体的多变量优化问题分解为多个单变量（或部分变量）的优化子问题，然后依次优化每个子问题，直至达到收敛）
如何减少时间和空间上的搜索复杂度，使规划聚焦于最优解可能出现的区域？
3.1 迭代优化计算方式求解流程

参考论文 Focused Trajectory Planning for Autonomous On-Road Driving

Step 1: 参考轨迹规划

考虑道路集合形状、障碍物，生成易于调整的类人参考时空轨迹。

Step 2: 跟踪轨迹规划

选择进一步满足运动学和动力学约束的参数化轨迹。

迭代优化计算方式求解流程

注意，与EM 存在不同：

EM 是 path --> speed --> path --> speed

该方法为：reference trajectory --> tracking trajectory --> reference trajectory --> tracking trajectory

3.2 参考轨迹生成 reference trajectory

1.种子路径生成

使用动态规划，生成无碰撞的最短路径。

优化目标：

\min_{\{e_{n_k \to n_{k+1}}\}} \sum_{k=0}^{K-1} C(e_{n_k \to n_{k+1}})

e_{n_k \to n_{k+1}} 是从节点 n_k 到 {k+1} n_{k+1} 的边。
C(e) 是代价函数。

代价函数：

C(e) = w_d \cdot d(e) + (1 - w_d) \cdot o(e) + \delta_{obstacle}(e)

w_d ：距离代价的权重。
d(e) ：路径的距离代价。
o(e) ：路径的平滑性代价（如曲率、转向角等）。
\delta_{obstacle}(e) ：碰撞代价（若路径与障碍物碰撞则有很高的代价）。

类似lattice 方法，点和点之间使用直线连接，得到种子路径。（apollo 中使用五次多项式，直接连的话，速度最快）

种子路径生成

2. 非参数化路径优化

散点优化：优化一系列点的位置，克服种子路径曲率不连续等问题。

目标：优化curvature，使用前后点差值计算，并转化为优化变量，对应散点的函数。

散点优化
优化路径

3. 速度分配

按照道路限速及最大向心加速度等限制，直接分配对应的速度。

但实际情况是，对于自动驾驶场景，可能没必要达到最大的限速以及最大向心加速度。可以使用speed optimization 进行速度调整。（比如apollo的 DP + QP）。

image.png

上面两步，其实就是 path + speed, 但是都是比较轻量化的计算，可以在一个cycle内完成。

3.3 跟踪轨迹生成 tracking trajectory

1. 参数化路径生成

基于散点，拟合出参数化路径。（不一定很有必要，如果上一步的resolution足够细，可以不用这步）对于大曲率场景，如停车场，U型弯等，可以使用参数化路径。

参数化路径生成

2. 跟踪轨迹采样与评估

轨迹采样--> 轨迹簇 --> 打分评估。

每个采样点在参考轨迹的周围，形成一组可能的轨迹候选

如果采样的空间越大，对参考轨迹的依赖越弱，越接近于全局路径采样。可能会找到更多时空联合的解。

采的越少，则越接近参考轨迹，其实是在局部采样。

速度生成
轨迹采样与评估
3.4 如何加速迭代计算方法规划效率？

核心：使用轻量化方法生成非参数化轨迹，并在参考轨迹附近聚焦搜索采样，减少计算量。

multi task： 做多条轨迹，比如line keep、lane change、nudge轨迹等。

加速规划效率
4.基于时空走廊的时空联合规划方法
4.1 时空走廊概念
论文链接：EPSILON: An Efficient Planning System for Automated Vehicles in Highly Interactive Environments
规划面临的问题：自动驾驶的语义要素具有不同形式的数学表征；保证规划轨迹的安全性和可行性。
语义时空走廊（Semantic Spatio-Temporal Semantic Corridor）一系列被语义要素约束的相互连接的无碰撞立方体。
三维时空走廊示意图

轨迹生成框架

时空走廊属于motion planning层级，用于生成轨迹。整体的时空联合轨迹决策规划框架流程如下：

轨迹生成框架

假设一辆自主驾驶汽车正在高速公路上行驶，前方有多辆车和一个即将到来的出口，自车需要在有限的时间内决定是否变道并驶入出口，同时避开周围的车辆。

1: 环境理解

环境理解模块首先从传感器（摄像头、激光雷达等）中获取当前的场景信息，包括：

静态环境：道路拓扑、车道线、路标、出口位置等。
动态物体：周围车辆的位置、速度、加速度等状态信息。

2: 预测模块

预测模块会基于当前的环境和动态物体的信息，生成每个动态物体的轨迹预测和行为预测：

轨迹预测：预测前方车辆未来几秒的可能位置。例如，假设前方车辆将保持当前车道并匀速行驶，预测车辆在未来几秒的位置。
行为预测：推测前方车辆的意图，比如某辆车可能打算变道，或者另一辆车可能会减速。

3: 行为规划（MPDM）

基于预测信息，行为规划模块生成多个可选的策略。

策略 1：继续保持当前车道，并在出口前方通过减速避开前车。
策略 2：加速变道到右侧车道，直接驶入出口。

每个策略都会生成一条对应的预期轨迹和行为方案，计算系统根据代价函数选择最优策略，比如考虑安全性、时间、能耗等因素。

4: 运动规划

一旦选择了最佳行为，则进入时空轨迹生成过程：

时空走廊生成：根据目标车道或障碍物，在三维时空图中划定一个走廊，确保车辆未来的运动限制在这个区域内，避免碰撞。
优化轨迹生成：在时空走廊中，系统利用优化算法（如梯度下降或QP）生成车辆的最优轨迹，考虑到平滑性、速度限制、加速度约束等。
路径：生成车辆的空间轨迹，确保它在物理上保持安全距离并满足道路约束。
速度：在空间路径的基础上，进一步优化速度曲线，确保车辆平稳地加速或减速，避免急刹或超速。

5: 轨迹跟踪与执行

最终生成的轨迹将作为输入传递给控制模块，控制模块根据轨迹调节车辆的方向、速度、加速等行为。

4.2 语义时空走廊的要素

1. 语义时空走廊的障碍物要素

核心思想：任何一个语义都可以抽象为时空下的约束或cost，并绑定到convex space 上, 转为QP问题。

比如 红灯：某个时间时，不能超过某个S。动态障碍物：某个时空下，不能碰到它的S、L。 变道：经过某个l时,总的时间要被限制，并且无碰撞。

语义时空走廊的障碍物要素

2. 语义时空走廊的约束要素

语义时空走廊的约束要素
4.3 语义时空走廊的构建

1. 语义时空走廊构建 ：输入数据

他车状态：从一个初始解出发，由MPDM得到，他车行为及轨迹的粗解；
静动态障碍物：通过时空走廊抽象出来；
语义边界：红绿灯、道路边界、变道时长、道路限速等；

根据初始解与约束条件，如何求解？

语义时空走廊构建 ：输入数据

2. 语义时空走廊构建：生成种子

初始解由behavior planning 模块中的MPDM过程给出，比较粗糙，可以保证无碰撞，但不一定保证顺滑。

生成种子

3. 语义时空走廊构建：膨胀立方体

初始生成的种子点已经避开了障碍物，但它们只是代表车辆能够行驶的初始路径。膨胀操作会根据障碍物的分布和车辆可能的未来状态，将初始路径扩展为一个安全走廊，使得车辆可以在一定的范围内自由调整，以应对动态变化。

膨胀立方体

4. 语义时空走廊构建：关联约束

关联约束（Constraint Association）包括了语义约束（例如交通规则、车道线约束）和速度约束等。在膨胀种子点并生成初步的时空走廊后，关联约束被引入来进一步减少路径选择的自由度。这些约束会收缩时空走廊的解空间，限制车辆行驶时的合法范围。

关联约束




衔接处问题：在不同的膨胀区域之间，关联约束需要确保两个区域能够平滑且可行地连接。如果衔接处处理不当，可能会导致路径不连续或存在过于急促的转折，影响车辆行驶的安全和稳定性。

衔接处平滑过渡：在两个膨胀区域的衔接处，系统需要选择靠近的种子点来实现衔接。通过对相邻种子点的合理选择，衔接处可以保持平滑的过渡，避免突兀的转弯或急剧的速度变化。
衔接处解空间收缩：辆在该区域的可行运动范围被限制在更小的空间内，从而减少了可能的路径选择。解空间的减少可以有效过滤掉不符合交通规则或动力学约束的路径，但同时也可能降低路径的灵活性，使得系统需要更加精细地处理衔接处的问题，确保生成的路径在有限的解空间内仍然保持最优。

5. 语义时空走廊构建：放宽边界

初步生成时空走廊时，由于硬约束（如障碍物、车道线）和关联约束的作用，解空间可能会变得过于狭窄，导致规划的路径不够灵活，或者无法找到可行解。

放宽边界

放宽边界的操作通过引入软约束，扩大了时空走廊的解空间，增加了轨迹规划的灵活性。虽然一些软约束可以在代价权衡下被适度违反，但硬约束（如避障和物理约束）始终严格保持不变，从而确保生成的路径在实际应用中是安全和可行的。

6. 其他问题

表达形式是否一定要是矩形，cube 形状？
对语义的遵从程度，与空间的自由度如何平衡，曲线在时空内过于顺滑，但是不类人？
基于走廊，如何生成轨迹。trajectory generation？
4.4 基于优化的轨迹生成

1. 贝塞尔曲线

采用参数优化的方法，基底选用贝塞尔曲线。通过优化控制点，决定曲线的表达形式。

贝塞尔曲线

定义： 贝塞尔曲线是一条由若干控制点决定的平滑曲线。控制点并不一定在曲线上，但它们的排列方式影响曲线的形状。

一条n阶贝塞尔曲线由 n+1 个控制点 (P_0, P_1, ..., P_n) 构成。曲线是这些控制点的加权平均，随着参数 (t) 从0到1逐步变化。
参数方程为： B(t) = \sum_{i=0}^{n} \binom{n}{i} (1 - t)^{n-i} t^i P_i
其中， (t) 是从 0 到 1 的参数， \binom{n}{i} 是二项式系数。
三次贝塞尔曲线
四次贝塞尔曲线

贝塞尔曲线特性：

起点与终点：贝塞尔曲线的起点和终点分别是第一个和最后一个控制点 P_0和 P_n 。
形状控制：中间的控制点 P_1, ..., P_{n-1}决定了曲线的形状。曲线不会严格经过这些点，但它的整体走向和曲率受它们的影响。
凸包性：曲线可以完全被限制在控制点组成的凸包内。 将控制点约束在convex space 内，则可保证生成的轨迹绝对安全。 --> 位置凸包。
Hodograph 性质： 贝塞尔曲线的导数（即速度向量）也是一条贝塞尔曲线，并由控制点决定，可将速度约束在一个合理的范围。--> 速度凸包。

贝塞尔曲线的凸包性和Hodograph 性质，这两个特性在路径规划与轨迹生成中尤为重要，尤其在保证安全性和控制速度的过程中起到了关键作用。

位置凸包：贝塞尔曲线的凸包性保证了路径在控制点构成的凸空间内。通过将控制点约束在安全的凸区域内，可以确保生成的轨迹在物理空间上是安全的，不会撞上障碍物。
速度凸包：贝塞尔曲线的Hodograph性质保证了速度的变化也受到控制点的约束。通过精确控制控制点，可以将速度变化约束在合理的范围内，确保车辆的加速和减速都在安全范围内。

2. 轨迹表示

曲线足够安全：约束每一段的控制点，在对应的凸包内，速度凸包、加速度凸包。

轨迹表示




3.轨迹优化问题

构造优化问题：最小化 jerk，转为 QP 问题，求解轨迹。

轨迹优化

4. 示例

5. EPSILON 代码

EPSILON 开源代码参考地址

5.1 SscPlanner::RunOnce() 主流程
EPSILON/util/ssc_planner/src/ssc_planner/http://ssc_planner.cc

SscPlanner::RunOnce() 是一个规划的主流程，包括：

获取车辆和环境信息（如自车状态、障碍物、轨迹等）
坐标系转换
构建时空走廊（Spatial-Temporal Corridor）
通过二次规划（QP）优化轨迹
根据优化结果更新最终的轨迹

1. 初始化与自车状态处理

stamp_ = map_itf_->GetTimeStamp();
if (map_itf_->GetEgoVehicle(&ego_vehicle_) != kSuccess) {
    LOG(ERROR) << "[Ssc]fail to get ego vehicle info.";
    return kWrongStatus;
}

获取当前时间戳和自车状态。
如果未能成功获取，函数会提前返回错误状态。

2. 初始状态设置

if (!has_initial_state_) {
    initial_state_ = ego_vehicle_.state();
}
has_initial_state_ = false;
is_lateral_independent_ = initial_state_.velocity > cfg_.planner_cfg().low_speed_threshold();

将自车状态设置为初始状态。
根据速度阈值决定是否启用横向独立规划。

3. 地图与障碍物信息获取

  if (map_itf_->GetLocalReferenceLane(&nav_lane_local_) != kSuccess) {
    LOG(ERROR) << "[Ssc]fail to find ego lane.";
    return kWrongStatus;
  }
  stf_ = common::StateTransformer(nav_lane_local_);

  if (stf_.GetFrenetStateFromState(initial_state_, &initial_frenet_state_) !=
      kSuccess) {
    LOG(ERROR) << "[Ssc]fail to get init state frenet state.";
    return kWrongStatus;
  }

  if (map_itf_->GetEgoDiscretBehavior(&ego_behavior_) != kSuccess) {
    LOG(ERROR) << "[Ssc]fail to get ego behavior.";
    return kWrongStatus;
  }

  if (map_itf_->GetObstacleMap(&grid_map_) != kSuccess) {
    LOG(ERROR) << "[Ssc]fail to get obstacle map.";
    return kWrongStatus;
  }

  if (map_itf_->GetObstacleGrids(&obstacle_grids_) != kSuccess) {
    LOG(ERROR) << "[Ssc]fail to get obstacle grids.";
    return kWrongStatus;
  }

  if (map_itf_->GetForwardTrajectories(&forward_behaviors_, &forward_trajs_,
                                       &surround_forward_trajs_) != kSuccess) {
    LOG(ERROR) << "[Ssc]fail to get forward trajectories.";
    return kWrongStatus;
  }

从地图接口获取自车车道、障碍物网格和前向轨迹等信息。
若任意一步失败，函数会提前返回。

4. 坐标转换

if (StateTransformForInputData() != kSuccess) {
    LOG(ERROR) << "[Ssc]fail to transform state into ff.";
    return kWrongStatus;
}

将自车状态从全局坐标系转换为Frenet 坐标。

5. 时空走廊构建

if (p_ssc_map_->ConstructSscMap(surround_forward_trajs_fs_[i], obstacle_grids_fs_)) {
    LOG(ERROR) << "[Ssc]fail to construct ssc map.";
    return kWrongStatus;
}
if (p_ssc_map_->ConstructCorridorUsingInitialTrajectory(p_ssc_map_->p_3d_grid(), forward_trajs_fs_[i]) != kSuccess) {
    LOG(ERROR) << "[Ssc]fail to construct corridor for behavior " << i;
    return kWrongStatus;
}

通过周围车辆的轨迹和障碍物信息构建一个时空走廊（corridor）。时空走廊是在轨迹生成中考虑时空上的可行空间。
为每个行为对应的轨迹在时空网格上生成一个安全走廊。

6. 轨迹优化

if (RunQpOptimization() != kSuccess) {
    LOG(ERROR) << "[Ssc]fail to optimize qp trajectories.\n";
    return kWrongStatus;
}

调用二次规划（QP）模块，生成平滑且可行的轨迹。
5.2 SscMap::ConstructCorridorUsingInitialTrajectory() 时空走廊的构建
EPSILON/util/ssc_planner/src/ssc_planner/http://ssc_map.cc

函数概述

SscMap::ConstructCorridorUsingInitialTrajectory 是构建时空走廊的关键函数。它基于初始轨迹和三维网格（GridMap3D），生成一组连续时空走廊，供后续规划出合理安全的轨迹。

函数分为两个阶段：

种子点生成（Seeds Generation）： 从轨迹中提取关键点，作为构建时空立方体的起点。
立方体膨胀与构建（Cube Inflation and Construction）： 利用种子点生成时空立方体，并膨胀以形成安全区域。

1. 阶段 I：种子点生成

vec_E<Vec3i> traj_seeds;
int num_states = static_cast<int>(trajs.size());

从输入的 Frenet 轨迹中提取坐标（s, d, t），并通过网格方法转化为离散种子点。
仅处理在范围内的点： if (!p_grid->CheckCoordInRange(coord_1)) { continue; }
确保点的时间坐标大于零（即点在当前时间之后）。

2. 阶段 II：立方体膨胀与构建

初始立方体生成

common::AxisAlignedCubeNd<int, 3> cube;
GetInitialCubeUsingSeed(traj_seeds[i], traj_seeds[i + 1], &cube);

基于相邻种子点生成初始立方体。
若初始立方体与障碍物冲突，则记录错误。

立方体膨胀

InflateCubeIn3dGrid(p_grid, dirs_disabled, config_.inflate_steps, &cube);

在 6 个方向（x、y、t）上扩展立方体。
膨胀步数由配置参数 config_.inflate_steps 决定。

立方体关联

如果下一个种子点位于当前立方体内，则直接关联
否则，截断当前立方体的时间边界，生成新的立方体并添加到走廊中。
if (CheckIfCubeContainsSeed(driving_corridor.cubes.back().cube, traj_seeds[i])) {
    driving_corridor.cubes.back().seeds.push_back(traj_seeds[i]);
    continue;
}

ErrorType SscMap::ConstructCorridorUsingInitialTrajectory(
    GridMap3D *p_grid, const vec_E<common::FsVehicle> &trajs) {
  // ~ Stage I: Get seeds
  vec_E<Vec3i> traj_seeds;  // 存储从轨迹中提取的离散种子点
  int num_states = static_cast<int>(trajs.size()); // 获取轨迹点的数量

  // 如果轨迹点数量大于 1，则开始提取种子点
  if (num_states > 1) {
    bool first_seed_determined = false; // 标记第一个种子点是否已确定
    for (int k = 0; k < num_states; ++k) {
      std::array<decimal_t, 3> p_w = {}; // 用于存储 Frenet 轨迹的 (s, d, t)

      if (!first_seed_determined) {
        // 提取初始状态的 Frenet 坐标
        decimal_t s_0 = initial_fs_.vec_s[0];
        decimal_t d_0 = initial_fs_.vec_dt[0];
        decimal_t t_0 = initial_fs_.time_stamp;
        std::array<decimal_t, 3> p_w_0 = {s_0, d_0, t_0};
        auto coord_0 = p_grid->GetCoordUsingGlobalPosition(p_w_0); // 转换为网格坐标

        // 提取当前轨迹点的 Frenet 坐标
        decimal_t s_1 = trajs[k].frenet_state.vec_s[0];
        decimal_t d_1 = trajs[k].frenet_state.vec_dt[0];
        decimal_t t_1 = trajs[k].frenet_state.time_stamp;
        std::array<decimal_t, 3> p_w_1 = {s_1, d_1, t_1};
        auto coord_1 = p_grid->GetCoordUsingGlobalPosition(p_w_1);

        // 跳过超出网格范围的轨迹点
        if (!p_grid->CheckCoordInRange(coord_1)) {
          continue;
        }

        // 跳过时间小于零的轨迹点
        if (coord_1[2] <= 0) {
          continue;
        }

        // 确定第一个种子点
        first_seed_determined = true;
        traj_seeds.push_back(Vec3i(coord_0[0], coord_0[1], coord_0[2]));
        traj_seeds.push_back(Vec3i(coord_1[0], coord_1[1], coord_1[2]));
      } else {
        // 提取后续轨迹点的 Frenet 坐标并转换为网格坐标
        decimal_t s = trajs[k].frenet_state.vec_s[0];
        decimal_t d = trajs[k].frenet_state.vec_dt[0];
        decimal_t t = trajs[k].frenet_state.time_stamp;
        p_w = {s, d, t};
        auto coord = p_grid->GetCoordUsingGlobalPosition(p_w);

        // 跳过超出网格范围的轨迹点
        if (!p_grid->CheckCoordInRange(coord)) {
          continue;
        }
        traj_seeds.push_back(Vec3i(coord[0], coord[1], coord[2]));
      }
    }
  }

  // ~ Stage II: Inflate cubes
  common::DrivingCorridor driving_corridor; // 用于存储生成的时空走廊
  bool is_valid = true; // 标记时空走廊是否有效
  auto seed_num = static_cast<int>(traj_seeds.size()); // 获取种子点的数量

  // 如果种子点不足两个，则构建失败
  if (seed_num < 2) {
    driving_corridor.is_valid = false;
    driving_corridor_vec_.push_back(driving_corridor);
    is_valid = false;
    return kWrongStatus;
  }

  // 遍历所有种子点，逐步生成时空立方体
  for (int i = 0; i < seed_num; ++i) {
    if (i == 0) {
      // 初始化第一个立方体
      common::AxisAlignedCubeNd<int, 3> cube;
      GetInitialCu


## 图片

![图片](https://picx.zhimg.com/v2-ddf658f2e6ef81ef4a5c8a6a53e2abf9_l.jpg?source=32738c0c&needBackground=1)

![图片](https://pic1.zhimg.com/v2-8ab856e72fbe4edb7642df4788e3055e_l.jpg?source=32738c0c&needBackground=1)

![图片](https://pic4.zhimg.com/v2-fa4e1de5a04ef24a7a72164f9bcf1d9d_1440w.jpg)

![图片](https://pic1.zhimg.com/v2-3bcc8242e912b51db8db609ba15e99f4_1440w.jpg)

![图片](https://pica.zhimg.com/v2-c79febe84a131a8a77fa35cd932eb16e_1440w.jpg)

![图片](https://pica.zhimg.com/v2-0bae16a66e2abc58f4a1737f3fc69d2a_1440w.jpg)

![图片](https://pic4.zhimg.com/v2-8c9f2ce3345a46b850710cbf917b1d65_1440w.jpg)

![图片](https://picx.zhimg.com/v2-694b39a2bc54530bceccb45b5bb0043d_1440w.jpg)

![图片](https://pic4.zhimg.com/v2-b95ea75fa77d088aa3af1a5354530da7_1440w.jpg)

![图片](https://pic1.zhimg.com/v2-6611a3e71aad6f9eea02726a274e103c_1440w.jpg)

![图片](https://pic1.zhimg.com/v2-46de06769bff762ece5ba2cc7e35af60_1440w.jpg)

![图片](https://pic4.zhimg.com/v2-ca3345c8d4e69e5f8c9fd0de7e998085_1440w.jpg)

![图片](https://pic1.zhimg.com/v2-52acca8f68a47e6fc9bd243a8e83b0c4_1440w.jpg)

![图片](https://pica.zhimg.com/v2-acc9694ab6de8caa70e03d2691c2ed86_1440w.jpg)

![图片](https://pic3.zhimg.com/v2-8ac80f59c34bf25c65b6442a1565ed36_1440w.jpg)

![图片](https://pic2.zhimg.com/v2-f5ec4115bd3171aa833b811b9186c98b_1440w.jpg)

![图片](https://pic1.zhimg.com/v2-d838d5014e09d478b767dce780ccbb80_1440w.jpg)

![图片](https://picx.zhimg.com/v2-8b5092d1d57e8d7f09159f610d6204a7_1440w.jpg)

![图片](https://picx.zhimg.com/v2-d5649bccc7ec02a620545e8853329ca5_1440w.jpg)

![图片](https://pic3.zhimg.com/v2-857b9cadd065ae89d7d65f922012de9c_1440w.jpg)

