---
title: "入门扩散模型"
author: "不是快递"
source_url: https://zhuanlan.zhihu.com/p/28106350800
platform: zhihu
collection: "学习收藏"
collection_url: https://www.zhihu.com/collection/713057152
topic: ""
subtopic: ""
status: pending
---

# 入门扩散模型

> 作者: 不是快递 | 来源: https://zhuanlan.zhihu.com/p/28106350800

---

系统整理相关优质的文章和教程，尽可能做到代码实现结合理论推导。会逐步更新，如有缺漏不足恳请指正

目录：
DDPM = 拆楼 + 建楼
DDPM = 贝叶斯+去噪
DDIM = 高观点下DDPM
流匹配Flow-Matching
修正流Rectified-Flow







（一）：DDPM = 拆楼 + 建楼

现阶段扩散模型发展起源，简短介绍

之前的工作和现在的主流的扩散模型几乎完全不一样。传统路径的扩散模型通过得分匹配等技术来训练能量模型，通过郎之万方程来执行从能量模型的采样，很难有效处理高分辨图像。基于15年发表的论文《Deep Unsupervised Learning using Nonequilibrium Thermodynamics》提出的数学框架，新的扩散模型DDPM首次在高分辨率图像生成上取得不错的效果。

总体流程

本质是利用训练好的神经网络将随机噪声z 变换成数据样本 x 的过程

重点在于，"拆楼"的过程十分直接，即一步一步添加随机噪声最终得到随机噪声 z，而随机噪声 z 直接到数据样本 x 十分困难，所以需要利用神经网络一步一步去还原(通过每一个中间过程的非完全去噪数据，训练出一个理解每一步该如何去噪的神经网络)

“拆楼的过程”

拆楼(前向传播) -> 重建(从多个 当前图像->拆一步后图像 数据对中，学习出 一个拆一步后图像->当前图像的模型) -> 参数优化(降低方差) -> 递归生成(从一个随机噪声出发T步完成图像生成)

/数据加噪"拆楼"

2025.3.5 待续。。。

"拆楼"的单步过程可定义为：

上式系数满足 \alpha_{t}^{2}+\beta_{t}^{2}=1 (耦合系数，可互相表示，为后续化简提供便利)，其中 \beta_{t}^{2} 接近0代表单步只拆一点楼。

反复执行这个拆楼的步骤：

根据 \alpha_{t}^{2}+\beta_{t}^{2}=1 结合正态分布的叠加性可对上式进行化简：

观察上式前两项，提出因式 \left( \alpha_{t}...\alpha_{2} \right)^{2} 可将前两项由 \left( \alpha_{t}...\alpha_{2} \right)^{2}\left( \alpha_{1}^{2}+\beta_{1}^{2}\right) 化简为 \left( \alpha_{t}...\alpha_{2} \right)^{2} ，然后与后一项结合，重复该操作，直至消去所有项得到1

最终得到：

选择适当的 \alpha_{t} 使得 \bar{ \alpha_{t}}\approx0 ，意味着经过T步后所剩的楼体几乎可以忽略(是否完全可以忽略？该残留是否有害？即使存在极微小的信息残留，反向去噪过程仍能通过模型学习到的先验知识重建数据，不会显著影响生成结果，并且可以证明这种残留对模型生成多样性是有益的)，已经全部转化为原材料ε

/重建，逐步还原

2025.3.6 待续。。。

根据 x_{t-1}\rightarrow x_{t} 的过程得到多个 \left( x_{t-1}\rightarrow x_{t} \right) 数据对，"重建"的过程就是从这些数据对中学习一个 x_{t}\rightarrow x_{t-1} 的模型 \mu\left( x_{t} \right) 。

逐步建模构建损失函数：

->首先最直接的可以想到最小化模型预测的 x_{t-1} 与实际 x_{t-1} 的欧式距离:

->根据式(3)可以将去噪前的图像用去噪后的图像表示 为x_{t-1}=\frac{1}{\alpha_{a}}\left( x_{t}-\beta_{t}\varepsilon_{t} \right) 。而模型预测的是当前图像去噪一步后的图像，结合 x_{t-1} 的表达式，只需要模型拟合一个预测的噪声 \epsilon_{\theta} 就可以用以单步重建图像，这个网络拟合的噪声以 x_{t} 和 t作为输入。 为了简化运算"建楼"模型 \mu\left( x_{t} \right) 可以设计为:

参数对称可以消去

->得到新的损失函数，其中 \theta 代表一系列可训练模型参数:

->进一步解析损失函数，将 x_{t} 用设置好的参数、随机噪声和初始图像表示出来。如果直接利用(6)式 x_{t}=\bar{\alpha}_{t}x_{0}+\bar{\beta}_{t}\varepsilon_{t} ，存在一个问题， \bar{\varepsilon}_{t} 中包含 {\varepsilon}_{t} ，二者并不相互独立，不是独立的话，可能会加大实现难度，甚至没法实现。比如，两个（独立的）高斯噪声，np.random.randn() 运行两次就行，这就是独立的好处；如果这两个噪声是不独立的，那么第一步np.random.randn()，第二步就要推导出它关于第一步采样结果的关系式，然后再去采样，因此先对 x_{t} 单步展开，再对展开式中 x_{t-1} 彻底展开得到与 \varepsilon_{t} 完全独立的 \bar{\varepsilon}_{t-1} :

->得到解析完所有参数的损失函数:

/降低方差优化损失函数

原则上来说，损失函数(11)就可以完成DDPM的训练，但它在实践中可能有方差过大的风险，从而导致收敛过慢等问题。

观察式(11)中实际包含了4个需要采样的随机变量

要采样的随机变量越多，就越难对损失函数做准确的估计，反过来说就是每次对损失函数进行估计的波动（方差）过大了。

我们可以通过一个积分技巧将 \varepsilon_{t}和 \bar{\varepsilon}_{t-1} 合并为单个正态分布随机变量从而缓解方差过大的问题

通过观察式(11)，其中 \alpha_{t}\bar{\beta}_{t-1}\bar{\varepsilon}_{t-1}+\beta_{t}\varepsilon_{t}=\bar{\beta}_{t}\varepsilon|\varepsilon\sim N\left( 0, 1 \right) 通过构造

\beta_{t}\bar{\varepsilon}_{t-1}-\alpha_{t}\bar{\beta}_{t-1}\varepsilon_{t}=\bar{\beta}_{t}\omega|\omega\sim N\left( 0, 1 \right) ，利用 \bar{\varepsilon}_{t-1} 和 {\varepsilon}_{t} 独立来验证 E\left[\varepsilon\omega^{\top} \right]=0 从而证明 \varepsilon 与 \omega 相互独立

联立两式将 \bar{\varepsilon}_{t-1} 消去，得到由 \varepsilon 和 \omega 表达的 \varepsilon_{t} :

带入(11)式:

展开化简:

上述公式等号最后一行中

(1) E_{\omega,\epsilon\sim N\left( 0, 1 \right)}\left\{ \eta^{\top}\eta \right\} 等于 E_{\epsilon\sim N\left( 0, 1 \right)}\left\{ ||\frac{\beta_{t}\epsilon}{\bar{\beta}_{t}}-\epsilon_{\theta}\left(\bar{\alpha}_{t}x_{0}+ \bar{\beta}_{t}\epsilon,t\right)||\right\}

(2) E_{\omega,\epsilon\sim N\left( 0, 1 \right)}\left\{ \omega^{\top}\omega\right\} 可以算出为常数 I

(3) E_{\omega,\epsilon\sim N\left( 0, 1 \right)}\left\{ \eta^{\top}\omega\right\} 通过定义：随机变量 \epsilon 与 \omega 相互独立，则他们各自的函数也相互独立。 \eta^{\top} 为关于 \epsilon 的函数 \frac{\beta_{t}\epsilon}{\bar{\beta}_{t}}-\epsilon_{\theta}\left(\bar{\alpha}_{t}x_{0}+ \bar{\beta}_{t}\epsilon,t\right) ，其中 \epsilon_{\theta}\left(\bar{\alpha}_{t}x_{0}+ \bar{\beta}_{t}\epsilon,t\right) 也为关于 \epsilon 得函数与 \omega 相独立，因此 E_{\omega,\epsilon\sim N\left( 0, 1 \right)}\left\{ \eta^{\top}\omega\right\} 为0

最终可得:

再次省掉常数和损失函数的权重，我们得到DDPM最终所用的损失函数:

/递归生成

2025.3.6 待续。。。

训练完之后，我们就可以从一个随机噪声 x_{T}\sim N\left( 0, I \right) 出发执行 T 步式(8)来进行生成：

这对应于自回归解码中的贪心搜索。果要进行随机采样，那么需要补上噪声项：(噪声就是增加多样性。并且所有的 x_{t}, x_{t-1}都是由真实图片加了特定强度的噪声得到的，而预测的时候， x_{t}却是由 x_{T}, x_{T-1}, ..., x_{t+1} (t表示当下的时间步，T表示最大时间步)一步步预测来的，所以要重新模拟回噪声强度，模拟训练场景)

在生成过程中可以让 \sigma_{t}=\beta_{t} ，对应正向和反向的方差一致。这个采样过程跟传统扩散模型的朗之万采样不一样的地方在于：DDPM的采样每次都从一个随机噪声出发，需要重复迭代T步来得到一个样本输出；朗之万采样则是从任意一个点出发，反复迭代无限步，理论上这个迭代无限步的过程中，就把所有数据样本都被生成过了。所以两者除了形式相似外，实质上是两个截然不同的模型。

从这个生成过程中，我们也可以感觉到它其实跟Seq2Seq的解码过程是一样的，都是串联式的自回归生成，所以生成速度是一个瓶颈，DDPM设了 T=1000 ，意味着每生成一个图片，需要将 \epsilon_{\theta}\left(x_{t}, t \right) 反复执行1000次，因此DDPM的一大缺点就是采样速度慢，后面有很多工作都致力于提升DDPM的采样速度。而说到“图片生成 + 自回归模型 + 很慢”，有些读者可能会联想到早期的PixelRNN、PixelCNN等模型，它们将图片生成转换成语言模型任务，所以同样也是递归地进行采样生成以及同样地慢。那么DDPM的这种自回归生成，跟PixelRNN/PixelCNN的自回归生成，又有什么实质区别呢？为什么PixelRNN/PixelCNN没大火起来，反而轮到了DD

了解PixelRNN/PixelCNN的读者都知道，这类生成模型是逐个像素逐个像素地生成图片的，而自回归生成是有序的，这就意味着我们要提前给图片的每个像素排好顺序，最终的生成效果跟这个顺序紧密相关。然而，目前这个顺序只能是人为地凭着经验来设计（这类经验的设计都统称为“Inductive Bias”），暂时找不到理论最优解。换句话说，PixelRNN/PixelCNN的生成效果很受Inductive Bias的影响。但DDPM不一样，它通过“拆楼”的方式重新定义了一个自回归方向，而对于所有的像素来说则都是平权的、无偏的，所以减少了Inductive Bias的影响，从而提升了效果。此外，DDPM生成的迭代步数是固定的T，而PixelRNN/PixelCNN则是等于图像分辨率（宽×高×通道数宽×高×通道数)，所以DDPM生成高分辨率图像的速度要比PixelRNN/PixelCNN快得多。


/超参设置

->在DDPM重构的时候我们用了欧氏距离(7)作为损失函数，而一般我们用DDPM做图片生成，但欧氏距离并不是图片真实程度的一个好的度量，VAE用欧氏距离来重构时，往往会得到模糊的结果，除非是输入输出的两张图片非常接近，用欧氏距离才能得到比较清晰的结果，所以选择尽可能大的T(原文设置为1000)，正是为了使得输入输出尽可能相近，减少欧氏距离带来的模糊问题。

->在加噪声拆解过程中，当t比较小时 x_{t} 还比较接近真实图片，所以我们要缩小 x_{t-1} 与 x_{t} 的差距，以便更适用欧氏距离(7)，因此要用较大的 \alpha_{t} ；当t比较大时， x_{t} 已经比较接近纯噪声了，噪声用欧式距离无妨，所以可以稍微增大 x_{t-1} 和 x_{t} 的差距，即可以用较小 \alpha_{t} 。这样可以得到 \alpha_{t} 是单调递减的满足：

如果用一直比较大的 \alpha_{t} 就需要增大T，而根据推导(6)的结论 (选择适当的 \alpha_{t} 使得 \bar{ \alpha_{t}}\approx0 ，这样经过T步后所剩的楼体才可以忽略)要满足 \bar{ \alpha_{t}}\approx0，可以估算：

带入T=1000， \bar{ \alpha_{t}}\approx e^{-5} ，这样就刚好达到\bar{ \alpha_{t}}\approx0

->最后我们留意到，“建楼”模型中的 \epsilon_{\theta}\left(\bar{\alpha}_{t}x_{0}+ \bar{\beta}_{t}\epsilon,t\right) 中，我们在输入中显式地写出了t，这是因为原则上不同的t处理的是不同层次的对象，所以应该用不同的重构模型，即应该有T个不同的重构模型才对，于是我们共享了所有重构模型的参数，将t作为条件传入。按照论文附录的说法，t转换成Transformer升级之路：1、Sinusoidal位置编码追根溯源 - 科学空间|Scientific Spaces介绍的位置编码后，直接加到残差模块上去的。


代码对应：

import torch


class DDPM():

    def __init__(self,
                 device,
                 n_steps: int,
                 min_beta: float = 0.0001,
                 max_beta: float = 0.02):
        betas = torch.linspace(min_beta, max_beta, n_steps).to(device)
        alphas = 1 - betas
        alpha_bars = torch.empty_like(alphas)
        product = 1
        for i, alpha in enumerate(alphas):
            product *= alpha
            alpha_bars[i] = product
        self.betas = betas
        self.n_steps = n_steps
        self.alphas = alphas
        self.alpha_bars = alpha_bars

    def sample_forward(self, x, t, eps=None):
        alpha_bar = self.alpha_bars[t].reshape(-1, 1, 1, 1)
        if eps is None:
            eps = torch.randn_like(x)
        res = eps * torch.sqrt(1 - alpha_bar) + torch.sqrt(alpha_bar) * x
        return res

    def sample_backward(self, img_shape, net, device, simple_var=True):
        x = torch.randn(img_shape).to(device)
        net = net.to(device)
        for t in range(self.n_steps - 1, -1, -1):
            x = self.sample_backward_step(x, t, net, simple_var)
        return x

    def sample_backward_step(self, x_t, t, net, simple_var=True):

        n = x_t.shape[0]
        t_tensor = torch.tensor([t] * n,
                                dtype=torch.long).to(x_t.device).unsqueeze(1)
        eps = net(x_t, t_tensor)

        if t == 0:
            noise = 0
        else:
            if simple_var:
                var = self.betas[t]
            else:
                var = (1 - self.alpha_bars[t - 1]) / (1 - self.alpha_bars[t]) * self.betas[t]
            noise = torch.randn_like(x_t)
            noise *= torch.sqrt(var)

        mean = (x_t - (1 - self.alphas[t]) / torch.sqrt(1 - self.alpha_bars[t]) * eps) / torch.sqrt(self.alphas[t])
        x_t = mean + noise

        return x_t







（二）：DDPM = 贝叶斯+去噪

本文介绍DDPM的另一种推导，它主要利用到了贝叶斯定理来简化计算，跟后面将要介绍的DDIM模型有着紧密的联系。

/回顾模型

正向由样本数据变为随机噪声，反向由随机噪声变为数据。其中反向过程就是训练得到的生成模型

正向扩散的过程，由采样输入和固定系数参数组成，后续可以化简为从原始图像至随机噪声一步到位

结合上式，用 x_{t-1} 表示 x_{t}的条件分布 p\left( x_{t}|x_{t-1} \right)=N\left( x_{t};\alpha_{t}x_{t-1}, \beta_{t}^{2}I\right) 。其中 \alpha_{t}x_{t-1} 为x_{t} 的均值，可以理解为 \alpha_{t}x_{t-1} 是x_{t} 的无噪声预测，叠加噪声 \beta_{t}^{2}I 得到带有少量随机噪声信息的x_{t}

在约束条件 \alpha_{t}^{2}+\beta_{t}^{2}=1 下：

可以改写 x_{0} 对 x_{t} 的条件分布为 p\left( x_{t}|x_{0} \right)=N\left( x_{t};\bar{\alpha}_{t}x_{0}, \bar{\beta}_{t}^{2}I\right) ，其中 \bar{\alpha}_{t}=\alpha_{1}...\alpha_{t} ， \bar{\beta}_{t}=\sqrt{1-\bar{\alpha}_{t}^{2}} 。

/结合贝叶斯展开去噪过程

根据贝叶斯定理直接求 x_{t} 条件下的 x_{t-1} 的条件概率(视为单步去噪过程)：

上式的 p\left(x_{t-1} \right) 和 p\left( x_{t} \right) 表达式是未知的，我们仅知道 在之前步加噪分布的条件下 之后步加噪分布的表达式，此时我们是知道在原始分布条件下任意去噪步的条件概率的，即 p\left( x_{t-1}|x_{0} \right) ， p\left( x_{t}|x_{0} \right) 均为已知。这样我们可以在额外给定 x_{0} 的条件下将 p\left( x_{t-1}|x_{t},x_{0} \right) 展开：

->p(a|b,c)==p(a|(b,c)): p\left( x_{t-1}|x_{t},x_{0} \right)=\frac{p\left( x_{t-1},x_{t},x_{0} \right)}{p\left( x_{t},x_{0} \right)}

->p(a,b,c)=p(b|(a,c))p(a,c): \frac{p\left( x_{t-1},x_{t},x_{0} \right)}{p\left( x_{t},x_{0} \right)}=\frac{p\left( x_{t}|x_{t-1},x_{0} \right)p\left( x_{t-1},x_{0} \right)}{p\left( x_{t},x_{0} \right)}

->p(b|(a,c))p(a,c)=p(b|(a,c))p(a|c)p(c): \frac{p\left( x_{t}|x_{t-1},x_{0} \right)p\left( x_{t-1},x_{0} \right)}{p\left( x_{t},x_{0} \right)}=\frac{p\left( x_{t}|x_{t-1},x_{0} \right)p\left( x_{t-1}|x_{0} \right)p\left( x_{0} \right)}{p\left( x_{t},x_{0} \right)}

->上下同除以 p\left( x_{0} \right) : \frac{p\left( x_{t}|x_{t-1},x_{0} \right)p\left( x_{t-1}|x_{0} \right)p\left( x_{0} \right)}{p\left( x_{t},x_{0} \right)}=\frac{p\left( x_{t}|x_{t-1},x_{0} \right)p\left( x_{t-1}|x_{0} \right)}{p\left( x_{t}|x_{0} \right)}

->正向过程被定义为马尔可夫链，即每一步仅依赖前一步，正向过程中 x_{t} 并未利用 x_{t-1} 以前步的信息，因此p\left( x_{t}|x_{t-1},x_{0} \right)=p\left( x_{t}|x_{t-1} \right)

带入已知的 p\left( x_{t-1}|x_{0} \right) ， p\left( x_{t}|x_{0} \right) ， p\left( x_{t}|x_{t-1} \right) ：

上式的推导：

p\left( x_{t}|x_{t-1} \right)=N\left( x_{t};\alpha_{t}x_{t-1},{\beta}_{t}^{2}I \right)

p\left( x_{t-1}|x_{0} \right)=N\left( x_{t-1};\bar{\alpha}_{t-1}x_{0},\bar{\beta}_{t-1}^{2}I \right)

p\left( x_{t}|x_{0} \right)=N\left( x_{t};\bar{\alpha}_{t}x_{0},\bar{\beta}_{t}^{2}I \right)

-> N\left( x;a_{1},bI \right) * N\left( y;a_{2},cI \right) = N\left( \frac{a_{1}c+a_{2}b}{c+b},\frac{cb}{c+b}I \right) : p\left( x_{t}|x_{t-1} \right)p\left( x_{t-1}|x_{0} \right)\sim N\left( \frac{\alpha_{t}x_{t-1}\bar{\beta}_{t-1}^{2}+\bar{\alpha}_{t-1}x_{0}\bar{\beta}_{t}^{2}}{\bar{\beta}_{t-1}^{2}+\bar{\beta}_{t}^{2}},\frac{\bar{\beta}_{t-1}^{2}\bar{\beta}_{t}^{2}}{\bar{\beta}_{t-1}^{2}+\bar{\beta}_{t}^{2}}I \right)

-> N\left( x;a_{1},bI \right) / N\left( y;a_{2},cI \right) = N\left( \frac{a_{1}c-a_{2}b}{c-b},\frac{cb}{c-b}I \right) (必须满足c>b): p\left( x_{t}|x_{t-1} \right)p\left( x_{t-1}|x_{0} \right) / p\left( x_{t}|x_{0} \right)\sim N\left( ... \right) 这样计算过于复杂

可以从密度函数的角度计算更简单：

f\left( x \right)=\frac{1}{\sigma\sqrt{2\pi}}e^{-\frac{\left( x-\mu \right)^{2}}{2\sigma^{2}}}

假设 p\left( x_{t-1}|x_{t},x_{0} \right) 的密度函数满足正态分布

带入 p\left( x_{t}|x_{t-1} \right) ， p\left( x_{t-1}|x_{0} \right) 和 p\left( x_{t}|x_{0} \right) 的密度函数，只看指数部分(指数乘除意味指数项的加减)去掉除掉公因子-1/2得指数项为：

根据一般正态分布指数项的表达式(略去-1/2对齐) \frac{\left( x-\mu \right)^{2}}{\sigma^{2}} ，(7)式中 x_{t-1}^{2} 项的系数为：

这样整理好的结果指数项的表达式必然是 \frac{\bar{\beta}_{t}^{2}}{\bar{\beta}_{t-1}^{2}\beta_{t}^{2}}||x_{t-1}-\tilde{\mu}\left( x_{t},x_{0} \right)||^{2} 的形式，这意味着协方差矩阵是 \frac{\bar{\beta}_{t-1}^{2}\beta_{t}^{2}}{\bar{\beta}_{t}^{2}}I 。另一边，把一次项系数拿出来是 -2\left( \frac{\alpha_{t}}{\beta_{t}^{2}}x_{t}+\frac{\bar{\alpha}_{t-1}}{\bar{\beta}_{t-1}^{2}}x_{0} \right) ，除以 \frac{-2\bar{\beta}_{t}^{2}}{\bar{\beta}_{t-1}^{2}\beta_{t}^{2}} 得到结果均值(可回代，配方验证假设)：

/去噪过程

现在有了 p\left( x_{t-1}|x_{t},x_{0} \right) ，但因为 x_{t} 到 x_{t-1} 的去噪过程是不依赖 x_{0} 的， x_{0} 是我们最终想要生成的结果，因此——如果我们能够通过 x_{t} 来预测 x_{0} ，那么就可以消去 p\left( x_{t-1}|x_{t},x_{0} \right) 中的 x_{0} ，使得它只依赖于 x_{t} 了这样，我们利用 \bar{\mu}\left( x_{t} \right) 来估计 x_{0} ，损失函数为 ||x_{0}-\bar{\mu}\left( x_{t} \right)||^{2} 。训练完成后，我们就认为：

在 ||x_{0}-\bar{\mu}\left( x_{t} \right)||^{2} 中， x_{0} 代表原始数据， x_{t} 代表带噪数据，所以这实际上在训练一个去噪模型，这也就是DDPM的第一个“D”的含义（Denoising）。具体来说，p\left( x_{t}|x_{0} \right)=N\left( x_{t};\bar{\alpha}_{t}x_{0}, \bar{\beta}_{t}^{2}I\right) ，意味着 x_{t}=\bar{\alpha}_{t}x_{0}+\bar{\beta}_{t}\varepsilon ， \varepsilon\sim N\left( 0,I \right) ，将 x_{0} 表达式求出 x_{0}=\frac{1}{\bar{\alpha}_{t}}\left( x_{t}-\bar{\beta}_{t}\varepsilon \right)， 这启发我们将 \bar{\mu}\left( x_{t} \right) 参数化为：

此时损失函数变为：

省去前面的系数，就得到DDPM原论文所用的损失函数了。可以发现，本文是直接得出了从 x_{t} 到 x_{0} 的去噪过程(直接训练x_{t} 到 x_{0}的损失函数)，而不是像之前两篇文章那样，通过 x_{t} 到 x_{t-1} 的去噪过程再加上积分变换来推导，相比之下本文的推导可谓更加一步到位了。

我们将式(11)代入到式(12)中，化简得到：

这就是反向的采样过程所用的分布，连同采样过程所用的方差也一并确定下来了。

推导过程：

p\left( x_{t-1}|x_{t} \right)\approx p\left( x_{t-1}|x_{t}, x_{0}=\bar{\mu}\left( x_{t} \right)\right)=N\left( x_{t-1}; \frac{\alpha_{t}\bar{\beta}_{t-1}^{2}}{\bar{\beta}_{t}^{2}}x_{t}+\frac{\bar{\alpha}_{t-1}\beta_{t}^{2}}{\bar{\beta}_{t}^{2}\bar{\alpha}_{t}}\left( x_{t}-\bar{\beta}_{t}\epsilon_{\theta}\left( x_{t},t \right) \right),\frac{\bar{\beta}_{t-1}^{2}\beta_{t}^{2}}{\bar{\beta}_{t}^{2}}I\right)

=N\left( x_{t-1}; \left( \frac{\alpha_{t}\bar{\beta}_{t-1}}{\bar{\beta}_{t}^{2}}+\frac{\bar{\alpha}_{t-1}\beta_{t}^{2}}{\bar{\alpha}_{t}\bar{\beta}_{t}^{2}}\right)x_{t}-\frac{\beta_{t}^{2}}{\alpha_{t}\bar{\beta}_{t}}\epsilon_{\theta}\left( x_{t},t\right),\frac{\bar{\beta}_{t-1}^{2}\beta_{t}^{2}}{\bar{\beta}_{t}^{2}}I\right)

主要是化简式中的 \frac{\alpha_{t}\bar{\beta}_{t-1}^{2}}{\bar{\beta}_{t}^{2}}+\frac{\bar{\alpha}_{t-1}\beta_{t}^{2}}{\bar{\alpha}_{t}\bar{\beta}_{t}}

/预估修正

扩散模型要做的事是想将 x_{T} 慢慢变成 x_{0} ，而我们在借用 p\left( x_{t-1}|x_{t},x_{0} \right) 近似 p\left( x_{t-1}|x_{t} \right) 时，却包含了"用 \bar{\mu}\left( x_{t} \right) 来预估 x_{0}"这一步，这样已经用 \bar{\mu}\left( x_{t} \right) 一步到位预估了，是否不需要逐步采样？

真实情况是"用 \bar{\mu}\left( x_{t} \right) 来预估 x_{0}"不会太准至少开始的相当多步内不会太准。它仅仅起到了一个前瞻性的预估作用，然后我们只用p\left( x_{t-1}|x_{t} \right)来推进一小步，这就是很多数值算法中的“预估-修正”思想，即我们用一个粗糙的解往前推很多步，然后利用这个粗糙的结果将最终结果推进一小步，以此来逐步获得更为精细的解。

/遗留问题

在上文结合贝叶斯展开去噪中，我们说式(4)没法直接用的原因是 p\left( x_{t-1}\right) 和 p\left( x_{t} \right) 均不知道。因为根据定义，我们有：

其中 p\left( x_{t}|x_{t} \right) 是知道的，而数据分布 \tilde{p}\left( x_{0} \right) 无法提前预知，所以不能进行计算。不过，有两个特殊的例子，是可以直接将两者算出来的

->第一个例子是整个数据集只有一个样本，不失一般性，假设该样本为0，此时 \tilde{p}\left( x_{0} \right) 为狄拉克分布 \delta\left( x_{0} \right) (用于描述概率质量或物理量完全集中在某一点的情况)，可以直接算出 p\left( x_{t} \right)=p\left( x_{t}|0 \right) 。继而代入式(4)，可以发现结果正好是 p\left( x_{t-1}|x_{t},x_{0} \right) ，式(5)取 x_{0}=0 的特例，即：

我们主要关心其方差为 \frac{\bar{\beta}_{t-1}^{2}\beta_{t}^{2}}{\bar{\beta}_{t}^{2}} ，这便是采样方差的选择之一。

->第二个例子是数据集服从标准正态分布，即 \tilde{p}\left( x_{0} \right)=N\left( x_{0};0,I \right)。前面我们说了 p\left( x_{t}|x_{t-1} \right)=N\left( x_{t};\alpha_{t}x_{t-1}, \beta_{t}^{2}I\right) 意味着 x_{t}=\bar{\alpha}_{t}x_{0}+\bar{\beta}_{t}\varepsilon ， \varepsilon\sim N\left( 0,I \right) ，而此时根据假设还有 x_{0} =N\left( 0,I \right) ，所以由正态分布的叠加性， x_{t} 正好也服从标准正态分布。将标准正态分布的概率密度代入式(4)，结果的指数部分除掉-1/2因子外，结果是：

跟推导 p\left( x_{t-1}|x_{t


## 图片

![图片](https://picx.zhimg.com/v2-ddf658f2e6ef81ef4a5c8a6a53e2abf9_l.jpg?source=32738c0c&needBackground=1)

![图片](https://picx.zhimg.com/v2-1a7e76cf444e1a308c550af191327f21_l.jpg?source=32738c0c&needBackground=1)

![图片](https://pic1.zhimg.com/v2-4812630bc27d642f7cafcd6cdeca3d7a.jpg?source=88ceefae)

![图片](https://pic2.zhimg.com/v2-9aef083bc347384cda5e9ce8c0981b23_1440w.jpg)

![图片](https://pic1.zhimg.com/v2-e29768a94a076a10b4fcb063965f73a4_1440w.jpg)

![图片](https://pic2.zhimg.com/v2-424aaf3d305c576737dd0ee20b0122a7_1440w.jpg)

![图片](https://pic4.zhimg.com/v2-01000e1f833d534ff1b6cb331c389787_1440w.jpg)

![图片](https://pic3.zhimg.com/v2-afbcc6aca947223c552010c3bbe847d0_1440w.jpg)

![图片](https://pic4.zhimg.com/v2-34c9dd1d5633d21b84e14f1a9d7b4cdb_1440w.jpg)

![图片](https://picx.zhimg.com/v2-92bc88a8ce88be16884bd5cb2d53d6af_1440w.jpg)

![图片](https://pic4.zhimg.com/v2-6264e1b2386eff7e70e7e7a3237c3bcd_1440w.jpg)

![图片](https://pic1.zhimg.com/v2-7acff500e6cc7ccd3aa25a9412c34884_1440w.jpg)

![图片](https://picx.zhimg.com/v2-2704b8804517010da5527f7c84ad32bf_1440w.jpg)

![图片](https://pic1.zhimg.com/v2-12bb3d3448264a2586fad78efd013cfe_1440w.jpg)

![图片](https://picx.zhimg.com/v2-f81c6b539fcb815d02dcb723547c5327_1440w.jpg)

![图片](https://pic1.zhimg.com/v2-f1467805d577c1382dd73384eb5f2d36_1440w.jpg)

![图片](https://picx.zhimg.com/v2-012709c24094a1c8e3a69826900a3e9b_1440w.jpg)

![图片](https://pic2.zhimg.com/v2-cb2e66452a2570adf5c2f206a4550641_1440w.jpg)

![图片](https://picx.zhimg.com/v2-c707b6e7ecfe0bea7033f6c8baead0a5_1440w.jpg)

![图片](https://picx.zhimg.com/v2-88bc34ce1ba717cb58d51b3c1b640cf7_1440w.jpg)

